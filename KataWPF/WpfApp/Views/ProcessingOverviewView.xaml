<UserControl x:Class="WpfApp.Views.ProcessingOverviewView"
             xmlns:my="clr-namespace:WpfApp.Icons"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\Resources\Manifest.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel Margin="10">
        <Border Height="30" Style="{StaticResource ShellViewRegionBackgroundBorderStyle}" CornerRadius="6">
            <TextBlock x:Name="Title" 
                       TextWrapping="Wrap" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Left" 
                       Margin="10,0,0,0"
                       FontFamily="Tahoma"
                       FontSize="16"
                       Foreground="White"
                       Text="Processing Overview"/>
        </Border>

        <Grid Margin="5,50,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
            </Grid.RowDefinitions>

            <TextBlock x:Name="ErrorSummary" Text="Tube scanning showed errors"/>
            <Button x:Name="View" Grid.Column="1" HorizontalAlignment="Right" Margin="0,0,30,0">
                <StackPanel>
                    <my:MagnifyingGlassIconControl />
                    <TextBlock Text="View"/>
                </StackPanel>
            </Button>
        </Grid>
            
        <Border x:Name="ErrorPanel" Style="{StaticResource ShellViewErrorBackgroundBorderStyle}" CornerRadius="6" Margin="0,20,0,0" Padding="5">
            <StackPanel>
                <TextBlock x:Name="BarcodeErrors" Text="2 barcode errors" Foreground="White"/>
                <TextBlock x:Name="WeighingErrors" Text="1 weighing error" Foreground="White"/>
            </StackPanel>
        </Border>
        
    </StackPanel>
</UserControl>