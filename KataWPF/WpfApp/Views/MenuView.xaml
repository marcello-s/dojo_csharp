<UserControl x:Class="WpfApp.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:WpfApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="350">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\Resources\Manifest.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Margin="0,0,2,0" Style="{StaticResource ShellViewRegionBackgroundBorderStyle}" CornerRadius="0,6,0,0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Button x:Name="Export">Export</Button>
                <Button x:Name="Import" Margin="5,0,0,0">Import</Button>
            </StackPanel>
            <Border x:Name="Notification" Grid.Column="1" Margin="5" Padding="5" CornerRadius="3" HorizontalAlignment="Right" Background="Yellow">
                <TextBlock x:Name="NotificationText" VerticalAlignment="Center" Text="Notification Text"/>
            </Border>
        </Grid>
    </Border>    
</UserControl>