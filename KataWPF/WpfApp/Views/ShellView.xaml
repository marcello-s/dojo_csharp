<UserControl x:Class="WpfApp.Views.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\Resources\Manifest.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Style="{StaticResource ShellViewBackgroundGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*" MinHeight="320"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="480"/>
            <ColumnDefinition Width="120"/>
        </Grid.ColumnDefinitions>

        <Canvas Grid.ColumnSpan="2" Style="{StaticResource ShellViewBackgroundCanvasStyle}">
            <TextBlock TextWrapping="Wrap" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10, 15, 0, 0" >
                <Run FontFamily="Tahoma" FontSize="18" Foreground="#FFFFFFFF" Text="Wizard 1.0"/>
            </TextBlock>
        </Canvas>

        <ContentControl Grid.Column="0" Grid.Row="1" x:Name="ActiveScreen"/>
        <ContentControl Grid.Column="1" Grid.Row="1" x:Name="NavigationVm"/>
        <ContentControl Grid.Column="0" Grid.Row="2" x:Name="MenuVm"/>

        <Border Grid.Column="1" Grid.Row="2" Margin="2,0,0,0" CornerRadius="6,0,0,0" Style="{StaticResource ShellViewRegionBackgroundBorderStyle}">
            <TextBlock TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Center" >
                <Run FontFamily="Tahoma" FontSize="12" Foreground="#FFFFFFFF" Text="© 2026 Dojo GmbH"/>
            </TextBlock>
        </Border>

    </Grid>
</UserControl>